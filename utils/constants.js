// Define each constant
const valuesForSpinner = [1, 5, 4, 2, 7, 10];
const valuesForTarget = [1, 1, 1, 1, 1, 1, 1, 1, 5, 5, 4, 4, 4, 2, 2, 2, 2, 2, 2, 7, 10];
const dailyCoinReward = 5;
const dailySpinReward = 3;
const bonusSignupCoins = 10;
const appPromotionCost = 5;
const appInstallRewardCoins = 5;

// Export each constant individually
module.exports = {
  valuesForSpinner,
  valuesForTarget,
  dailyCoinReward,
  dailySpinReward,
  bonusSignupCoins,
  appPromotionCost,
  appInstallRewardCoins,
};
